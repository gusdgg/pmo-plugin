columns:
    status:
        label: Estado
        type: text
    name:
        label: Idea
        type: text
    code:
        label: Código
        type: text
    
    requestor:
        label: Solicitante
        relation: requestor
        select: concat(first_name, ' ', last_name)
    pilar:
        label: pilar
        relation: pilar
        select: name
    supervisor:
        label: supervisor
        relation: supervisor
        select: concat(first_name, ' ', last_name)
#    situacion:
#        label: situacion
#        type: text
#    mejoras:
#        label: mejoras
#        type: text
    objetivo:
        label: objetivo
        type: text
#    alcance:
#        label: alcance
#        type: text
    created_by:
        label: Creada por
        relation: creator
        select: concat(first_name, ' ', last_name)
    pm:
        label: PM
        relation: pm
        select: concat(first_name, ' ', last_name)
    created_at:
        label: Creada el
        type: datetime
        format: d-m-Y
